# 设计系统使用指南

**版本**: 2.0（情感化设计）  
**更新日期**: 2026年2月2日

---

## 快速开始

### 1. 颜色使用

```swift
// ✅ 正确：使用主色调渐变
Button("保存") { }
    .background(Color.primaryGradient)

// ✅ 正确：使用语义化颜色
Text("成功").foregroundStyle(Color.statusSuccess)

// ❌ 错误：硬编码颜色
Text("标题").foregroundStyle(Color(hex: "#FF0000"))
```

### 2. 卡片组件

```swift
// 标准卡片
EmotionalCard(style: .default) {
    Text("内容")
}

// 重要信息卡片（浮起效果）
EmotionalCard(style: .elevated) {
    Text("整体情况")
}

// 友好提示卡片（温暖背景）
EmotionalCard(style: .gentle) {
    Text("💡 今日建议")
}

// 警告卡片
EmotionalCard(style: .warning) {
    Text("⚠️ MOH风险")
}

// 成功卡片
EmotionalCard(style: .success) {
    Text("✅ 完成任务")
}

// 向后兼容（旧代码仍可使用）
InfoCard {
    Text("内容")
}
```

### 3. 动画使用

```swift
// ✅ 使用情感化动画
withAnimation(EmotionalAnimation.gentleConfirm) {
    isComplete = true
}

// ✅ 呼吸动画（主按钮）
.scaleEffect(isBreathing ? 1.1 : 1.0)
.animation(EmotionalAnimation.breathe, value: isBreathing)

// ✅ 流体动画（进度条）
.animation(EmotionalAnimation.fluid, value: progress)
```

### 4. 触觉反馈

```swift
// 轻柔反馈（选择、切换）
Button("选择") {
    let impact = UIImpactFeedbackGenerator(style: .light)
    impact.impactOccurred()
}

// 成功反馈（保存完成）
Button("保存") {
    saveData()
    let notification = UINotificationFeedbackGenerator()
    notification.notificationOccurred(.success)
}
```

### 5. 可访问性

```swift
// ✅ 按钮辅助功能
Button { } label: {
    Image(systemName: "trash")
}
.accessibilityLabel("删除")
.accessibilityHint("双击删除此记录")
.minimumTouchTarget() // 确保44x44pt

// ✅ 可选择标签
SelectableChip(
    label: "压力",
    isSelected: $isSelected,
    accessibilityHint: "点击选择或取消选择此诱因"
)
```

---

## 组件速查表

### 按钮组件

| 组件 | 用途 | 最小尺寸 |
|------|------|----------|
| `PrimaryButton` | 主要操作 | 高度56pt |
| `SecondaryButton` | 次要操作 | 高度56pt |
| `IconButton` | 图标操作 | 44x44pt |
| `SelectableChip` | 多选标签 | 高度32pt |
| `BreathingRecordButton` | 首页主按钮 | 120pt直径 |

### 卡片组件

| 组件 | 样式 | 使用场景 |
|------|------|----------|
| `EmotionalCard(.default)` | 标准 | 普通信息展示 |
| `EmotionalCard(.elevated)` | 浮起 | 重要信息、概览 |
| `EmotionalCard(.gentle)` | 柔和 | 友好提示、建议 |
| `EmotionalCard(.warning)` | 预警 | MOH风险、警告 |
| `EmotionalCard(.success)` | 成功 | 正向反馈、完成 |

### 输入组件

| 组件 | 用途 | 特性 |
|------|------|------|
| `CircularSlider` | 疼痛强度 | 360度拖动、触觉反馈 |
| `PainIntensitySlider` | 疼痛强度（线性） | 传统滑块 |
| `HeadMapView` | 疼痛部位 | 交互式头部地图 |
| `SelectableChip` | 多选项 | 带图标、触觉反馈 |

### 反馈组件

| 组件 | 用途 | 动画 |
|------|------|------|
| `EncouragingText` | 鼓励文案 | 出现动画 |
| `AnimatedNumber` | 数字变化 | 滚动动画 |
| `LoadingView` | 加载状态 | 呼吸动画 |
| `ToastView` | 轻提示 | 滑入+淡出 |

---

## 颜色速查表

### 主色调

```swift
Color.accentPrimary      // #5EC4B6 治愈蓝绿
Color.accentSecondary    // #4A90E2 柔和蓝
Color.warmAccent         // #F4A261 温暖橙
Color.gentlePink         // #E8A0BF 柔和粉
```

### 渐变

```swift
Color.primaryGradient    // 蓝绿→蓝（主按钮）
Color.warmGradient       // 橙→黄（正向提示）
```

### 疼痛强度色阶

```swift
0-2:  #A8D5BA (薄荷绿)
3-4:  #F4E285 (柔黄)
5-6:  #F4B860 (温橙)
7-8:  #E89A81 (珊瑚)
9-10: #D17A7A (柔红)
```

### 语义色

```swift
Color.statusSuccess    // 成功/安全
Color.statusWarning    // 警告/注意
Color.statusError      // 错误/危险
Color.statusInfo       // 信息/提示
```

---

## 动画速查表

### 时长标准

| 动画类型 | 时长 | 使用场景 |
|----------|------|----------|
| 快速 | 0.15s | 按钮按压 |
| 标准 | 0.3s | 页面切换 |
| 舒缓 | 0.5s | 数据加载 |
| 呼吸 | 2.0s | 待机动画 |
| 数据滚动 | 1.0s | 数字变化 |

### 动画曲线

```swift
// 快速响应
.easeInOut(duration: 0.2)

// 弹簧动画（自然感）
.spring(response: 0.4, dampingFraction: 0.8)

// 呼吸效果
.easeInOut(duration: 2.0).repeatForever(autoreverses: true)
```

---

## 间距使用指南

### 8pt网格系统

```swift
Spacing.xxs  // 4pt  - 极小间距
Spacing.xs   // 8pt  - 列表项内元素
Spacing.sm   // 12pt - 卡片内padding
Spacing.md   // 16pt - 页面边距
Spacing.lg   // 24pt - 卡片之间
Spacing.xl   // 32pt - Section之间
Spacing.xxl  // 48pt - 页面顶部
```

### 圆角规范

```swift
CornerRadius.sm   // 8pt  - 小标签
CornerRadius.md   // 12pt - 卡片
CornerRadius.lg   // 16pt - 大卡片
CornerRadius.xl   // 24pt - 按钮
CornerRadius.full // 999pt - 圆形
```

---

## 常见问题

### Q1: 什么时候用EmotionalCard，什么时候用普通View？

**使用EmotionalCard的场景**:
- 需要突出显示的信息块
- 可点击的内容区域
- 需要统一样式的模块

**使用普通View的场景**:
- 简单的HStack/VStack布局
- 不需要背景和阴影的内容
- 列表行（已有List样式）

### Q2: 如何选择卡片样式？

```swift
.default  → 80%的情况（标准信息）
.elevated → 页面最重要的内容（如首页状态卡片）
.gentle   → 友好提示、建议、智能推荐
.warning  → 需要注意的信息（MOH风险）
.success  → 正向反馈、完成状态
```

### Q3: 动画会不会太多？

**不会！我们已考虑**:
- ✅ 检测"减弱动画"设置
- ✅ 动画时长控制在0.3s以内（除呼吸动画）
- ✅ 仅在必要时使用（反馈、过渡）
- ✅ 避免连续动画（不会让用户眩晕）

### Q4: 颜色会不会太亮刺眼？

**不会！我们的优化**:
- ✅ 降低了所有颜色的饱和度
- ✅ 增加了色彩的温度感
- ✅ 在暗黑背景下测试过
- ✅ 考虑了畏光患者的需求

### Q5: 如何集成Lucide图标？

**步骤**:
1. 从 https://lucide.dev/ 下载SVG
2. 拖入 `Assets.xcassets/Lucide/` 文件夹
3. 设置为Template Image
4. 使用 `LucideIcon.heart.image`

**当前状态**:
- 枚举已创建（60+图标）
- SF Symbols作为后备（已实现）
- 可选：手动下载SVG（可在后续实施）

---

## 最佳实践

### DO ✅

- 使用设计系统定义的颜色、间距、圆角
- 为所有交互添加触觉反馈
- 为所有按钮添加VoiceOver标签
- 使用情感化文案（鼓励、温暖）
- 添加loading和空状态
- 测试暗黑模式下的视觉效果

### DON'T ❌

- 硬编码颜色值
- 忽略最小触摸目标尺寸
- 使用恐慌性语言（"高危险"）
- 过度使用动画（会眩晕）
- 跳过VoiceOver测试
- 仅依赖颜色传递信息

---

## 代码示例：完整页面

```swift
struct ExampleView: View {
    @State private var value = 5
    
    var body: some View {
        ScrollView {
            VStack(spacing: Spacing.lg) {
                // 1. 标题
                Text("示例页面")
                    .font(.title.bold())
                    .foregroundStyle(Color.primaryGradient)
                    .frame(maxWidth: .infinity, alignment: .leading)
                
                // 2. 重要信息卡片
                EmotionalCard(style: .elevated) {
                    VStack(alignment: .leading, spacing: 12) {
                        Text("重要信息")
                            .font(.headline)
                        Text("这是页面的核心内容")
                            .font(.body)
                            .foregroundStyle(Color.textSecondary)
                    }
                }
                
                // 3. 友好提示
                EncouragingText(type: .custom(
                    text: "你做得很好！",
                    icon: "star.fill"
                ))
                
                // 4. 数据展示
                EmotionalCard(style: .default) {
                    HStack {
                        AnimatedNumber(value: value)
                            .font(.system(size: 48, weight: .bold))
                        Spacer()
                    }
                }
                
                // 5. 操作按钮
                Button {
                    value += 1
                    let impact = UIImpactFeedbackGenerator(style: .light)
                    impact.impactOccurred()
                } label: {
                    Text("增加")
                        .frame(maxWidth: .infinity)
                        .frame(height: 56)
                        .background(Color.primaryGradient)
                        .foregroundStyle(.white)
                        .cornerRadius(16)
                }
                .buttonStyle(GentlePressStyle())
            }
            .padding()
        }
        .background(Color.backgroundPrimary)
    }
}
```

---

## 迁移指南

### 从旧版本迁移

**颜色**:
```swift
// 旧版
Color.accentColor → Color.accentPrimary

// 保持兼容
AppColors.primary → 仍然有效（别名）
```

**卡片**:
```swift
// 旧版（仍然有效）
InfoCard { }

// 新版（推荐）
EmotionalCard(style: .default) { }
```

**动画**:
```swift
// 旧版
AppAnimation.standard

// 新版（更语义化）
EmotionalAnimation.gentleConfirm
EmotionalAnimation.breathe
```

---

## Cheat Sheet

### 快速复制代码片段

**鼓励性文案**:
```swift
EncouragingText(type: .streak(days: 7))
EncouragingText(type: .firstRecord)
EncouragingText(type: .improvement)
```

**呼吸按钮**:
```swift
@State private var isBreathing = false

Circle()
    .scaleEffect(isBreathing ? 1.1 : 1.0)
    .animation(EmotionalAnimation.breathe, value: isBreathing)
    .onAppear { isBreathing = true }
```

**数字滚动**:
```swift
AnimatedNumber(value: streakDays)
    .font(.largeTitle.bold())
```

**触觉反馈**:
```swift
let impact = UIImpactFeedbackGenerator(style: .light)
impact.impactOccurred()
```

**渐变按钮**:
```swift
.background(Color.primaryGradient)
.foregroundStyle(.white)
.cornerRadius(16)
.shadow(color: Color.accentPrimary.opacity(0.3), radius: 8)
```

---

## 性能注意事项

### 动画优化

```swift
// ✅ 使用drawingGroup()提升复杂动画性能
Circle()
    .trim(from: 0, to: progress)
    .stroke(lineWidth: 8)
    .drawingGroup()

// ✅ 降低动画频率（30fps足够）
TimelineView(.animation(minimumInterval: 1/30)) { }
```

### 避免过度重绘

```swift
// ✅ 使用@StateObject而非@State创建ViewModel
@StateObject private var viewModel = MyViewModel()

// ✅ 使用equatable避免不必要的更新
.onChange(of: value) { old, new in
    guard old != new else { return }
    // 处理变化
}
```

---

## 测试清单

在提交代码前，请确保：

- [ ] 在真机上测试（模拟器动画可能不准确）
- [ ] 测试暗黑模式下的所有页面
- [ ] 测试不同字体大小（设置 → 显示与亮度 → 文字大小）
- [ ] 启用VoiceOver测试导航
- [ ] 启用"减弱动画"测试
- [ ] 在iPhone SE和Pro Max上测试布局
- [ ] 测试iPad分栏布局（如果支持）

---

## 设计评审标准

### 视觉一致性
- 间距是8的倍数
- 圆角符合规范（8/12/16/24）
- 颜色来自设计系统
- 字体使用系统定义

### 情感化
- 文案友好、鼓励
- 动画舒缓、流畅
- 反馈及时、柔和
- 视觉温暖、治愈

### 可访问性
- 所有按钮≥44x44pt
- VoiceOver标签完整
- 色彩+图标/文字组合
- 支持动态字体

---

**维护者**: Design Team  
**问题反馈**: 请在项目issue中提出  
**设计资源**: 见 `/docs/UI_UX优化实施总结.md`
